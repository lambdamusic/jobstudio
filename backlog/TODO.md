# TODO

<!-- Numbering: `#N` is a permanent ID assigned once when an item is added — never
     renumbered, never reused (checked-off or removed items keep/retire their number).
     Position in the file reflects section grouping, not creation order.
     Next available: #35 -->

## Architecture

- [ ] `#23` **Verify `#1`'s agent-native flows actually work** — try `/jobs-search scan` (default live scoring, and `--api`) and `/jobs-search cv` (tailoring, plus `--rebuild`/`--import`) for real, not just the code-level smoke tests done while building them. Confirm the rendered report/CV output looks right, not just that the commands run without erroring.
  - [x] `scan` default (agent-native) — verified 2026-09-17 via the real `2026-09-17-portal-scan.md` run (well-formed Strong/Other tables, rubric-grounded notes).
  - [x] `scan --api` — verified 2026-09-17 on a small (2-candidate) test run; **found and fixed a real bug**: `score-candidates.py`'s `rubric.format()` crashed (`KeyError: 'placeholder'`) because the rubric file's own explanatory header contained a literal `{placeholder}` in prose, not an actual substitution slot — the agent-native path never hit this since it reads the file as plain text rather than through `str.format()`. Fixed by rewording the header in `src/prompts/scan_score_rubric.md` to avoid literal braces. `--api` had never been run live since the 2026-09-17 scoring-split refactor.
  - [x] `cv` tailoring, functional mode — verified against a real application in the tracker, including the new Summary-rewrite rule.
  - [ ] `cv` tailoring, chronological mode — not yet run for real
  - [ ] `cv --rebuild` (interview bootstrap) — not yet run for real
  - [ ] `cv --import <path>` — not yet run for real
- [x] `#1` ~~**Drop direct Anthropic API calls**~~ — done 2026-09-17. CV tailoring (`subcommands/cv.md`) and scan's default scoring (`subcommands/scan.md`) are both agent-native now, no API call. Scan keeps a deliberate opt-in API path (`scan --api` → `src/score-candidates.py`) since agent-native scoring spends Claude Code session/rate-limit budget rather than API dollars — worth keeping as scan volume grows (`#8`/`#9`); `scan-portals.py` → `score-candidates.py` chained also stays fully headless/cron-able, and both scoring paths share one rubric file (`src/prompts/scan_score_rubric.md`) so methodology can't drift. Cover-letter structures consolidated to standard-only (Standard 1 default, new Standard 2 alternative), dropping mirror/advanced. `cv --rebuild` (interview) / `cv --import <path>` bootstrap the two base CVs from scratch or an existing document (`cv.md` §A, exact markdown contract in `references/cv-base-format.md`) — not yet exercised live, worth a real run before trusting it fully. Ties into `#2`'s onboarding story. See `backlog/done/drop-direct-anthropic-api-plan.md`
- [ ] `#2` **Idea:** turn this whole repo into a shareable toolkit anybody could run — detach Michele's personal data (CV content, company tracker, applications, profile/criteria, notes) from the codebase, and verify a genuine "start from scratch" setup works for a new person. Plan fleshed out 2026-09-20 with decisions closed and milestones drafted — see `backlog/share-as-toolkit-plan.md`. **Named 2026-09-21: ships as `jobstudio`** (unhyphenated, one spelling everywhere — plan §2e), renamed in **M1.11** after M1.4 and before the public repo is cut. Shape: **M1** public GitHub template (external data root + `config.yaml` identity + `tools/py` + synthesised `example-data/` + `init`), **M2** pip package, **M3** drive Claude Code from the Django app (recommended stretch goal — independent of M1/M2, prototypable now), **M4** Claude Desktop/Cowork (researched, parked). Second audit found personal data in git is ~4× worse than first thought (~690 of 810 tracked files, incl. `exports/` and `site/`), so the public repo must be a fresh repo with fresh history. **M1.1–M1.7 and M1.9–M1.11 are all done as of 2026-09-21, and M1.8 is two-thirds done — the automated and clean-room passes are both green (see `log/2026-09-21-m18-cleanroom-verification.md`). The clean room found nothing wrong with the install or the README; it did find two stale exemptions in `make-public-tree` that were letting real company names ship in `tests.py`, now fixed. Only "a second real person" is left, which needs someone else's machine and time rather than more work here.** The toolkit is `dev2/jobstudio`, the data is `_Jobs/jobstudio-data`, and the old repo is a frozen read-only archive (plan §5.7).

- [x] `#26` **Delete the retired sandbox** ✅ **Done 2026-09-21** — Michele deleted `~/dev2/jobsearch-sandbox` (52MB) by hand, along with the duplicate example data root `jobstudio-devdata2`. Nothing pointed at either: `~/.jobstudio.ini`, the workspace file and `.claude/settings.local.json` were all checked and repointed first, and the sandbox's one genuinely unique file — `backlog/m1.1-inventory.md`, written there during M1.1 and referenced by `tools/make-public-tree` — was rescued into `backlog/` at M1.7. A file-by-file comparison of its `backlog/` and `log/` against the archive confirmed nothing else existed only there. Its `data-root-move` branch stays on GitHub as history. `jobstudio-devdata` is kept deliberately as the scratch root for exercising the example data.

  Original entry: the sandbox held ~33MB of real personal data — 34 application folders, the 448KB database with 47 applications, `exports/`, and the 7.7MB `extras/` archive — an unbacked-up second copy of the job search that would have silently drifted from the real one.

## Portal scanning

- [x] `#3` **Portal scanning (Phase 1)** — `jobs-search scan` / `src/scan-portals.py` rescans tracked companies via public ATS APIs, scores matches against target-area profiles, reports to `jobs/scans/`. Nothing auto-writes to the tracker.
- [x] `#4` **Portal scanning — two-dimension scoring + hard location filter** — every match gets a CV Score (fit vs `jobs/targets/*.yaml`) and an Ideal-Job Score (fit vs `jobs/notes/ideal-job-notes.md`, the personal notes on what makes work satisfying), rendered as a table. Non-UK/non-remote roles are excluded from Matches entirely (not just capped) — shown only as a "Discarded" count.
- [x] `#5` **Portal scanning — extended ATS coverage** — Greenhouse, Lever, Workable, Workday, Ashby, SmartRecruiters, Teamtailor, Rippling ATS, plus a one-off first-party fetcher for a single company with a bespoke careers page (8 platforms total). `company_overrides` in `<DATA>/jobs/scan-config.yaml` maps company name → real board/tenant/domain for companies whose tracked URL is a generic corporate page hiding the real ATS (externalised from `scan-portals.py` on 2026-09-21 — it was one person's company list sitting in shared code). Coverage: 29/44 tracked companies scanned. The 15 remaining are not scanned for a specific confirmed reason each, recorded per company in `known_unsupported` in that same file: one on Dayforce (Cloudflare-protected — deliberately not bypassed); one each on UKG, BambooHR, SuccessFactors and Webitrent (not integrated, or no public API at all); one not currently hiring; one whose board token is unconfirmed; and eight with genuinely bespoke careers pages where no ATS could be detected.
- [x] `#6` **Portal scanning — quality fixes** — full pagination with server-side keyword filtering (was capping Workday/SmartRecruiters at 20/100, missing real matches); chunked scoring calls (was silently failing on large batches, mis-categorizing real matches as "needs review"); hyperlinked company names in "No open roles found"/"Not scanned" (was plain text); "Filtered out before scoring" section (postings that fail the local keyword pre-filter were vanishing with zero visibility).
- [ ] `#7` **Portal scanning — LinkedIn** — investigate a legitimate way to include LinkedIn postings (direct automation is ToS-hostile/technically fragile — login wall, bot detection — so this likely means a different approach: manual-forward workflow, or checking if a compliant partner API exists, not scraping)
- [ ] `#8` **Portal scanning — other general job portals** — add a broader job-board source beyond company-by-company tracking (e.g. Adzuna or Reed.co.uk, both have free public APIs with strong UK coverage) so scanning isn't limited to companies already tracked. This is real discovery (new companies/roles), unlike the current company-rescan approach.
- [ ] `#9` **Portal scanning (discovery)** — search for new companies/roles matching target areas, not just recheck tracked ones. Flag as unvetted since these skip the manual sanity check tracked companies already had. (Related to the general-job-portal item above — may end up being the same piece of work.)
- [ ] `#10` **Portal scanning — remaining platforms** — BambooHR, SuccessFactors and UKG: platform identified but no fetcher built (1 tracked company each, low priority unless requested)
- [x] `#11` ~~**Report / summary command** — a `jobs-search report` subcommand that produces a richer narrative summary of the search state (inspired by career-ops report function)~~ — closed 2026-09-17: no longer relevant, `jobs-search status` already covers this
- [ ] `#22` Create custom parsers for selected companies in the long tail (bespoke career pages with no public jobs API — see `## Not scanned` in any portal scan report for the current list)
- [x] `#30` **Show scan coverage on the companies list** — done 2026-09-23. `/companies/` said nothing about whether a company is actually being rescanned, and the only way to find out was to open the latest scan report and read its "Not scanned" section. Each row now shows the ATS it is scanned on, or the specific reason it isn't, with a count in the page lede.

  The predicate existed but was unreachable: `resolve_source()` lived in `src/scan-portals.py`, whose hyphen makes it unimportable. Moved it, the ATS URL patterns, and the "not scanned" reasons into a new `src/scan_sources.py`, and **`scan-portals.py` now calls the same `coverage()` the page does** — the point of the move was not access but agreement: two copies of these reasons would drift, and a coverage column that disagrees with the report is worse than no column. Not `scan_config.py`, whose docstring is explicit that it holds one person's settings; Greenhouse URL shapes are toolkit knowledge.

  The reasons stay graded rather than collapsed into one "not scanned": "no URL on record" is a gap anyone can close in a minute, a `known_unsupported` entry is a confirmed platform with no fetcher written (`#10`), and the bespoke fallback means nobody has found an API (`#22` — this column is exactly that list). Shown as text under the marker, not a tooltip, per the visible-over-hover preference from `#25`.
- [x] `#34` **Link out to the careers page from the Scan column when a company isn't scanned** — done 2026-09-23. A `careers ↗` link now follows the reason text on exactly the rows where scanning can't help, so checking a bespoke board is one click from the list rather than a trip through the company page.

  Kept off the scanned rows (already watched automatically — the link there would be noise rather than signal) and off rows with no URL on record, which have nothing to link to; that reason is itself the prompt to go and fill the URL in. `target="_blank" rel="noopener"`, matching `Site ↗` on the dashboard and `Careers page ↗` on the company detail page. `test_a_careers_link_appears_only_where_scanning_cannot_help` asserts both halves — present where wanted, absent where not — so the link can't quietly spread to every row.

## CV variants
- [x] `#13` ~~**Idea:** create `analytics-leadership` variant — less research/scholarly focus, more emphasis on analytics leadership, BI/dashboards, translating customer needs into insights. Revisit when 3–5 strong-fit roles in this space emerge (e.g. analytics director/VP in media, finance, consumer tech)~~ — closed 2026-09-17: no longer needed
- [x] `#14` **Idea:** drop CV variants entirely — not very useful as standalone files. Better captured as indications per target area; variants would be created at runtime for each CV instead — done 2026-09-17: pre-generated `jobs/cv/variants/` library retired, replaced by two base CVs (functional/chronological) tailored once per application on demand (see `backlog/done/simplify-cv-pipeline-plan.md`)

## Pipeline
- [x] `#16` Simplify CV pipeline to two tailored-per-application base CVs (functional default, chronological opt-in via `Application.cv_base`), dropping the pre-generated variant library — done 2026-09-17, see `backlog/done/simplify-cv-pipeline-plan.md`

## Web app / admin site
- [x] `#17` Make external links open in a new tab/window (e.g. company detail page `/companies/<slug>/` renders notes.md with several external source URLs — currently they navigate away from the app in-place) — done 2026-09-17: chained the existing `blank_links` filter (already used on scan reports) onto every `|markdown` render across the templates — company/area/note/profile pages and all of application detail (record, job description, my notes, CV, cover letters, other files)
- [x] `#18` Reorder the application detail page tabs (`src/web/apps/tracker/templates/tracker/application_detail.html`) to: Record, Job description, My notes, CV, Cover letters, History — done 2026-09-17. "Other files" and "Also at `<company>`" stayed after History.
- [x] `#24` Add a button on the application detail page to change status to "Reviewing" directly (no trip to Django admin). As a result, rename the existing "Edit/Change Status" button to "Edit in Admin". — done 2026-09-17: new local-only `/actions/mark-reviewing/<num>/` view + button (hidden once already `reviewing`); existing admin-link button relabelled "Edit in admin" to match the house lowercase-button-label convention. Verified live in-browser, including the History tab logging the transition via the existing post-save signal.
- [x] `#25` ~~**Relative dates on all application-listing pages**~~ — done 2026-09-18: `_app_table.html`'s Date column (shared by every application-listing page — active/applied/all/status/area via `application_list.html`, plus `company_detail.html`, `area_list.html`, and `application_detail.html`'s siblings table) now always shows both — the relative form ("N days/weeks ago", via the existing `ago` filter) as the main text with the exact `Y-m-d` date as a `<span class="sub">` subtitle underneath, same two-line pattern already used for the Role column's summary. Chose the subtitle over a hover tooltip since it's visible without hovering (Michele's preference). Removed the now-redundant `relative_date` flag (home.html's dashboard table was the only caller and always wanted the relative form anyway).

- [x] `#28` **Turn "Mark reviewing" into a multi-choice status control** — done 2026-09-23. The application detail page offered exactly one local-only status action, a hardcoded jump to `reviewing`; every other transition meant opening the Django admin. It now carries a **Set status** menu listing all seven statuses in `STATUS_SORT_ORDER` bar the one it is already in.

  `views.mark_reviewing` → `views.set_status(request, num, status)` at `/actions/set-status/<num>/<status>/`, still local-only, still saving through `Application.save()` so `status_order` and the post-save History log behave as they do for an admin edit. The status is checked against `STATUS_SORT_ORDER` rather than trusted — it arrives from the URL, so an unknown value 404s instead of writing a status nothing else in the app understands.

  **UI shape: a `<details>` dropdown** (Michele's call, 2026-09-23), over a row of six always-visible buttons — seven statuses would crowd the actions row and wrap on a narrow window. `<details>` opens natively, so the menu still works with JavaScript off, the same bargain `tabs.js` makes. Verified in-browser end to end, History tab included.

- [x] `#31` **Icons for the sidebar's main items** — done 2026-09-23. Nine line glyphs (Dashboard, Applications, Companies, Areas, CVs, Profile, Notes, Portal scans, Admin), defined once as `<g id="i-…">` symbols in an inline sprite at the top of `base.html` and drawn with `<use>`. Same-document symbols rather than an external sprite file: one less thing for the static build to copy, and one less way for the icons to vanish from the published mirror.

  `stroke="currentColor"` is what makes them inherit the active item's navy and the local-only grey for free, rather than needing a second set of rules. Sub-items stayed text-only as planned, and their indent was retuned (8px padding + 16px icon + 8px gap = 32px) so their labels line up with the labels above rather than the glyphs. `.nav-group a` moved off `justify-content: space-between`, which would have pushed each glyph away from its own label; the count and the local-only badge are pushed right individually instead. A test checks every `<use href>` resolves to a symbol that exists — a typo there renders nothing at all, silently.
- [x] `#32` **Rename "Job search" → "Job Studio", and make the sidebar title distinctive** — done 2026-09-23. **Both open questions answered by Michele: "Job Studio" (two words) is the display name, and the rename covers every page, not just the sidebar.** The display name is deliberately not the package name — repo, command and package stay `jobstudio`, unhyphenated, per `#2`/plan §2e.

  The name was written out 15 times across 13 templates plus `admin.site_header`, so it now lives once in `settings.APP_NAME`, exposed through the existing `site_context` processor — the same consolidation that file already did for `EDITOR_URL_SCHEME`. Each template's `{% block title %}` dropped its ` · Job search` suffix; `base.html` appends the name once, so page titles are consistent now and renaming again is a one-line change. Distinctiveness: **one inline-SVG lockup** — an outline briefcase glyph with a gold lens, then the two-tone wordmark (navy "Job", gold "Studio") drawn beside it in the same SVG, with the owner/year line underneath. Six variants were mocked up and reviewed in-browser; Michele picked this glyph in this layout. A lockup keeps glyph and words fixed relative to each other rather than being two elements that drift at different zooms, and inline rather than an icon file so it cannot go missing from the published mirror. The words are **drawn, not interpolated from `APP_NAME`** — a fixed-width SVG would overrun its viewBox on a longer name — so `BrandTests.test_the_wordmark_matches_the_app_name` fails if the two ever disagree.
- [x] `#33` **Link the GitHub repo from the sidebar footer** — done 2026-09-23. `https://github.com/lambdamusic/jobstudio`, beside the existing "Local · sqlite" / "Published" marker. Deliberately **not** behind `{% if IS_LOCAL %}`, unlike every other sidebar link: the published mirror is where it earns its place, since a reader who likes the site has no other way to find out what built it. `test_the_repo_link_survives_publishing` asserts it in both environments, so a later tidy-up that sweeps the footer behind the local guard fails rather than quietly removing it.

## File naming
- [ ] `#27` **Re-order the application-folder naming convention to `{type}-{date-created}-{username}-{job-slug}`** — e.g. `cover-letter-2026-09-18-michele-pasin-046-bbc-studios.md`, replacing today's `{job-slug}-{username}-{type}-{date}` (`046-bbc-studios-michele-pasin-cover-letter-2026-09-18.md`, the 2026-09-08 convention in `src/appfolder.py`'s `app_filename()`). Putting the type first makes a folder listing sort by kind rather than by a company name every file in the folder already repeats. **Also apply it to `notes.md` and `job.md`**, which are currently bare names with no date or owner at all — so `notes-2026-09-18-michele-pasin-046-bbc-studios.md`, `job-2026-09-18-michele-pasin-046-bbc-studios.md`.

  Touches more than `app_filename()`: the recognisers next to it (`is_cv_filename`, `is_cover_letter_filename`, `is_tailored_cv`) and the web parsers in `src/web/apps/tracker/parsers.py` must keep matching **both** old and new names — real application folders already on disk are not renamed — and `notes.md`/`job.md` are currently looked up by exact filename in the parsers, the skill (`subcommands/application.md`), `appfolder.py`'s scaffolding and `src/web/README.md`'s file-map table. Decide up front whether existing folders get a migration pass or are simply left alone; check `src/web/apps/tracker/tests.py` fixtures either way.

## Company tracking
- [ ] `#20` Write long-form research files (`jobs/companies/<slug>.md`) for the 64 companies added 2026-09-16 by that extraction — deferred as a separate step at Michele's request; do on demand as each one is actually worth considering, following the template in `jobs/companies/README.md`
- [ ] `#21` **Idea:** "find similar companies" — pick a company already in the tracker and surface other companies (same space/positioning/stage) worth shortlisting, as a discovery mechanism. Manually prototyped 2026-09-14: given a tracked company in robotics/physical-AI data with no UK presence, asked for similar UK-based alternatives and found one with the same "data layer for physical AI" positioning and a London HQ, via a plain web search — worth automating as a repeatable step (`jobs-search` subcommand or an option on `company`) rather than redoing this research by hand each time
